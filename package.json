{
  "name": "copilot-vault-agent",
  "version": "1.0.0",
  "description": "Obsidian plugin: chat with your vault via GitHub Copilot / GitHub Models",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "tslib": "^2.8.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@github/copilot-sdk": "^0.2.2"
  }
}
